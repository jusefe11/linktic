apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: registry-cert
  namespace: registry
spec:
  secretName: registry-tls

  issuerRef:
    name: local-ca-issuer
    kind: ClusterIssuer

  dnsNames:
  - registry.local
