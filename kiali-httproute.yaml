apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kiali
  namespace: istio-system
spec:
  parentRefs:
  - name: platform-gateway-istio
    namespace: istio-system
  hostnames:
  - kiali.local
  rules:
  - backendRefs:
    - name: kiali
      port: 20001
